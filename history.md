# 작업 히스토리

> 집/회사 등 여러 PC의 Claude Code가 작업 맥락을 이어가기 위한 일자별 기록.
> 상세 구조·함수 위치는 [Docs/HANDOFF.md](Docs/HANDOFF.md) 참고.
> 저장소: `yona1210/tubble2026yona` (GitHub Pages 배포). 전달은 이 저장소 push/pull로만 이뤄짐.

---

## 2026-06-25 (집)

### 한 일

1. **배포 파일명 정리 → `index.html`** (커밋 `4bea22c`)
   - GitHub Pages가 루트의 `index.html`을 자동 서빙하도록 배포 파일 이름을 `index.html`로 변경.
   - 내용은 `터블_관리시스템_v67_보안저장발주연동개선.html`(v67)과 동일한 단일 HTML.

2. **페이지 타이틀 변경 테스트 → 되돌림**
   - 커밋 `3548623`: 타이틀을 "드래곤아야쓰사"로 바꿔보는 테스트.
   - 커밋 `ba1b9e8`(원격): 타이틀을 다시 "터블 상품 관리 시스템"으로 **되돌림(Revert)**.
   - 현재 파일의 `<title>`은 `터블 상품 관리 시스템 v67 · 보안·저장·발주 연동 개선`.

3. **CSS 색상 변수화 리팩토링** ⚠️ *아직 커밋 안 됨 (working tree)*
   - 목적: 하드코딩된 색상값(`#fff` 등)을 CSS 변수로 추출 → 테마 일관성 / 향후 다크모드·테마 대응 기반 마련.
   - `:root`에 변수 신규 추가: `--card`(카드 배경), `--ink2`(보조 글자), `--soft2`(연한 배경), `--tint-alert` / `--tint-warn` / `--tint-good`(경고·주의·정상 틴트).
   - `background:#fff` → `var(--card)`, 글자색 → `var(--ink)`, 경고행 배경 → `var(--tint-alert)` 등 전반 치환.
   - 규모: `index.html` 약 278줄 변경 (+167 / -111). **표시 로직·발주 산식·데이터 구조 변경 없음, 스타일만 교체.**
   - `터블_관리시스템_v67_보안저장발주연동개선.html`(원본)에도 동일 작업 반영.

### 현재 상태 (회사에서 받기 전 확인할 것)

- ⚠️ **로컬이 원격(origin/main)보다 1커밋 뒤처짐(behind 1)** — 원격에 타이틀 Revert 커밋(`ba1b9e8`)이 있음. 회사에서 작업 전 `git pull` 필요.
- ⚠️ **CSS 변수화 작업은 미커밋 상태**(`index.html` MM). 집에서 `git add` → `commit` → `push` 하지 않으면 회사 PC로 전달되지 않음.
- 브라우저 실제 렌더 육안 검수는 아직 안 함(작업 환경에 Chromium 없음). 변수 치환 후 색상 깨짐 없는지 회사에서 한 번 열어 확인 권장.

### 다음 작업 후보

1. CSS 변수화 커밋 & push (전달 위해 필수).
2. 변수 치환 후 화면 색상 육안 검수.
3. (HANDOFF 기준) demandAnalysis 죽은 재할당 정밀 제거(P2), 매출·리뷰 엑셀 업로드 차트 실데이터 검수.
